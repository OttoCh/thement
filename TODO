1. STUDENT
  # backend
  // 1st milestone
  [-] registration
  [-] login
  [-] settings (email, password)   => email (no need)
  [-] profile (user detail, image) => image upload still fails => buggy on loading

  // 2nd milestone
  [ ] db schema
  [ ] choosing lecturer
      STATUS :
      1) none     => not yet choosing, REQUIRED : 
      2) pending  => chosen, not accepted/rejected yet, REQUIRED :
      3) accepted => chosen, accepted, REQUIRED :
  [ ] verification

  # frontend
  [-] registration
  [-] login
  [-] dashbo
  [-] profile
  [ ] settings (notification)       => alert on wrong still fails

2. LECTURER
  # backend
  [] login
  [] forget password

  # frontend

3. ADMIN
  # backend

  # frontend

4. KAPRODI
  # backend

  # frontend

5. OTHER
